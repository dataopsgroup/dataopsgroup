
import React from 'react';

const ChatbotSection = () => {
  // Botpress integration temporarily removed due to broken URLs causing SEO issues
  // The previous implementation used dummy URLs that were causing 403 errors
  // TODO: Re-implement with proper Botpress configuration when valid URLs are available
  
  return <></>;
};

export default ChatbotSection;
